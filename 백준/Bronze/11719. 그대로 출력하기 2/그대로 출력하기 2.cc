#include <bits/stdc++.h>
using namespace std;
string s;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(0);
	while (getline(cin, s)) {
		cout << s << '\n';
		// eof
		if (cin.eof()) break;
	}
	return 0;
}