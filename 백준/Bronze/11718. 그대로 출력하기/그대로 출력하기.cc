#include <bits/stdc++.h>
using namespace std;
string s;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(false);
	while (getline(cin, s) && s.length() != 0) {
		cout << s << "\n";
		s.clear();
	}
	return 0;
}