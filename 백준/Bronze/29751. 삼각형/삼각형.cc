#include <bits/stdc++.h>
using namespace std;
float w, h, a;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(false);
	cin >> w >> h;
	a = w * h / 2;
	cout << fixed << setprecision(1) << a << endl;
	return 0;
}