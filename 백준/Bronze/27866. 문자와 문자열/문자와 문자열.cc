#include <bits/stdc++.h>
using namespace std;
string str;
int i;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(false);
	cin >> str;
	cin >> i;
	cout << str[i - 1] << '\n';
	return 0;
}