#include <bits/stdc++.h>
using namespace std;
int d1, d2;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(0);
	cin >> d1 >> d2;
	cout << fixed << setprecision(6);
	cout << (float)((float)d1 * (float)2) + (float)(3.141592 * (float)d2 * (float)2);
	return 0;
}