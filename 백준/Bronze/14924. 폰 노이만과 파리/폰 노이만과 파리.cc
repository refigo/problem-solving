#include <bits/stdc++.h>
using namespace std;
int s, t, d;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(false);
	cin >> s >> t >> d;
	cout << d / (s * 2) * t << endl;
	return 0;
}