#include <bits/stdc++.h>
using namespace std;
int a, b;
int main(){
	cin >> a >> b;
	cout << setprecision(numeric_limits<double>::digits10 + 1);
	cout << (double)((double)a / (double)b) << '\n';
	return 0;
}
