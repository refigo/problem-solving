#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n, alpha;
    if (!(cin >> n)) return 0;
    if (!(cin >> alpha)) return 0;
    cout << 90 << '\n';
    return 0;
}