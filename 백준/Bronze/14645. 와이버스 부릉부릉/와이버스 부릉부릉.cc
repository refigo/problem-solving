#include <bits/stdc++.h>
using namespace std;
int n, k;
int main() {
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(false);
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		cin >> k >> k;
	}
	cout << "비와이\n";
	return 0;
}